#!/usr/bin/env sh

if [ -n "${DEVCONTAINER_TAB_TITLE:-}" ] || [ -f /.dockerenv ] || grep -qaE '/docker/|/kubepods/|/containerd/' /proc/1/cgroup 2>/dev/null; then
  exit 1
fi

hostname -s 2>/dev/null || hostname
